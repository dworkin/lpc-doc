NAME
	pow - compute power

SYNOPSIS
	float pow(float x, float y)

DESCRIPTION
	Return x to the power y.

ERRORS
	An error will result if 0.0 is taken to a negative power, if a negative
	number is taken to a non-integer power, or if the result is too large.

EXAMPLES
	float x;

	x = pow(10, 5);
	write("X = pow(10, 5) = " + x + "\n");

SEE ALSO
	kfun/acos, kfun/asin, kfun/atan, kfun/atan2, kfun/ceil, kfun/cos,
	kfun/cosh, kfun/exp, kfun/fabs, kfun/floor, kfun/fmod, kfun/modf,
	kfun/frexp, kfun/ldexp, kfun/log, kfun/log10,
	kfun/tan, kfun/tanh, kfun/sin, kfun/sinh, kfun/sqrt, kfun/random

