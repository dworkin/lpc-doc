NAME
	make_dir - create a directory

SYNOPSIS
	int make_dir(string dir)

DESCRIPTION
	Create a new directory.  1 is returned if the directory could be
	created, 0 otherwise.

EXAMPLES
	string filename;

	filename = "/tmp/oldstuff";
	if (make_dir(filename)) {
		write("Made directory: " + filename + "\n");
	}

SEE ALSO
	kfun/editor, kfun/get_dir, kfun/query_editor, kfun/read_file,
	kfun/remove_dir, kfun/remove_file, kfun/rename_file, kfun/write_file

