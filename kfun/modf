NAME
	modf - compute floating point remainder

SYNOPSIS
	float *modf(float x)

DESCRIPTION
	Split the argument into a fraction f and an integer part n, such that
	| f | < 1.0, and f + n == x.  ({ f, n }) is returned.  Note that
	n is returned as a float, and may not be representable in type int.

EXAMPLES
	float *x;

	x = modf(50003);
	write("X[0] = " + x[0] + " X[1] = " + x[1] + "\n");

SEE ALSO
	kfun/acos, kfun/asin, kfun/atan, kfun/atan2, kfun/ceil, kfun/cos,
	kfun/cosh, kfun/exp, kfun/fabs, kfun/floor, kfun/fmod,
	kfun/frexp, kfun/ldexp, kfun/log, kfun/log10, kfun/pow,
	kfun/tan, kfun/tanh, kfun/sin, kfun/sinh, kfun/sqrt, kfun/random

